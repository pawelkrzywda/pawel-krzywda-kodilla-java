EXPLAIN SELECT *
        FROM READERS
        WHERE FIRSTNAME = 'John';

CREATE INDEX BOOKTITLE ON BOOKS (TITLE);
CREATE INDEX READERSNAMES ON READERS (FIRSTNAME, LASTNAME);

SELECT * FROM READERS WHERE FIRSTNAME = 'John';

EXPLAIN SELECT *
        FROM READERS
        WHERE FIRSTNAME = 'John';